{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cat >> \"${TMPDIR:-/tmp}/dbaudit-run-${CLAUDE_CODE_SESSION_ID:-unknown}.jsonl\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
